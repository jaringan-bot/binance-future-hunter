{
  "name": "whale-stream-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Always-on Binance !forceOrder@arr + !contractInfo consumer for whalescope-mcp",
  "scripts": {
    "start": "node --experimental-sqlite --disable-warning=ExperimentalWarning index.mjs",
    "test": "node --experimental-sqlite --disable-warning=ExperimentalWarning --test"
  }
}
