---
description: Roadmap rebrand Stage 0–4 vs pipeline Tahap 1–5
alwaysApply: true
---

# Roadmap Rebrand (≠ Tahap Pipeline 1–5)

`docs/full_pipeline_framework.md` Tahap 1–5 (hard screen → capital solve) **sudah di kode merged**. Jangan disamakan dengan Stage rebrand di bawah.

## Stage 0–4 — ✅ DEPLOYED (2026-09-02)

PR #1 (`a343004`) + PR #2 (`c522e37`) → `main`. Worker live `e511dcad`.
`tsc` bersih · 849 src test · 51 stream-gateway test.

- **0** rebrand → `binance-future-hunter` ✅
- **1** CFTC trend + `binance_analyze_institutional_flow` (mig 0012) ✅
- **2** outcomes cron (mig 0013), execution-aware backtest (`fee_bps`/`slippage_bps`),
  `scripts/calibrate-ranking-weights.mjs`, ranking sub-scores (mig 0014) ✅
- **3** `binance_watch_orderbook_realtime` — on-demand depth watch WS `@depth@100ms`
  (`stream-gateway/depthWatch.mjs`), live-verified ✅
- **4** `src/notify.ts` multi-channel (Telegram/Discord/webhook) + dashboard
  read-only (`/dashboard`, `/api/dashboard/*`) ✅

D1 = instance existing (`database_id 3600a9bb-…`), migrations 0001–0014 applied
`--remote`. VPS AWS `svm-vps` (`13.212.7.132`): proxy + stream-gateway + Caddy.

Detail per-task + hasil live-verify: `docs/superpowers/plans/2026-09-02-stage-3-4-realtime-dashboard.md` (status block atas — **Semeru yang maintain**).

## Backlog (tunggu acc user — jangan mulai)

- **Relay REST kedua** (Krakatau) — mitigasi weight-ban IP tunggal
  (`whale-binance-proxy` 1 IP pernah kena `-1003`). Deploy instance ke-2
  `proxy-standalone/` di host BEDA (Fly.io/Deno/VPS ke-2, **BUKAN** Vercel)
  + `PROXY_URL_2`/`PROXY_SECRET_2`. Setelah failover verified live: Semeru
  naikkan `ENTRY_WATCHLIST_SIZE` (`src/entryWatchlist.ts`) 250 → 350–500.
- **Whale-wallet-discovery** — spec `docs/superpowers/specs/2026-09-01-whale-wallet-discovery.md` (belum jadi Task, butuh verifikasi endpoint live dulu).
- **Provision D1/KV baru** — dibatalkan (lanjut instance existing).

## Deferred

- Leverage Bracket riil — butuh API key Binance + desain Semeru.

## Follow-up tuning (BUKAN bug — tercatat di plan file)

- Ambang wall depth-watch `$250k` kekecilan buat buku BTC (churn tinggi) —
  pertimbangkan per-symbol / skala-volume.
- `EVENT_BUFFER_PER_SYMBOL=500` penuh cepat di pair likuid.

Jangan mulai fitur baru tanpa spec/task eksplisit dari user.
