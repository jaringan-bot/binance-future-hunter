---
description: Roadmap rebrand Stage 0–4 vs pipeline Tahap 1–5
alwaysApply: true
---

# Roadmap Rebrand (≠ Tahap Pipeline 1–5)

`docs/full_pipeline_framework.md` Tahap 1–5 (hard screen → capital solve) **sudah di kode merged**. Jangan disamakan dengan Stage rebrand di bawah.

## Stage 0–2 — kode selesai, BELUM di-commit/deploy

Kode + test lokal hijau (`typecheck` bersih, ~800 test). Semua masih di working tree `main` (untracked/modified). **Belum:** commit, push, PR, `d1 migrations apply --remote`, `wrangler deploy`. Itu tugas Cursor setelah user acc.

- **0011** (`pipeline_decision_log`) = **SUDAH merged** (`8beca7b`, PR #36)
- **0012/0013** = **BELUM** — `?? migrations/0012_cftc_positioning_history.sql`, `?? migrations/0013_pipeline_decision_outcomes.sql`

**Isi Stage 0–2 (untracked):**
- CFTC `cftc_positioning_history` + `cme_get_institutional_positioning_trend` + `cftcPositioningCron`
- `binance_analyze_institutional_flow` (`src/institutionalFlow.ts`, `src/tools/institutionalFlow.ts`)
- `pipelineDecisionOutcomeCron` (backfill `forward_return_1h/4h/24h`, `sl_touched_24h`)
- `scripts/calibrate-ranking-weights.mjs` + test — standalone, input JSON export manual, **TIDAK auto-apply**
  - GAP follow-up: 4 sub-skor komponen belum kolom D1 → migration 0014 / `scoreTier1Signals` breakdown
- backtest execution-aware: `fee_bps` (def 4) + `slippage_bps` (def 2), `net = gross − 2·(fee+slip)/1e4`

**Jangan tulis "deployed"** di docs/rules sampai commit + migrate remote + deploy terverifikasi.

## Stage 3 — belum ditulis

VPS/stream-gateway enhancements. Spec: `docs/superpowers/specs/2026-09-01-whale-wallet-discovery.md`.
- Opsi A direkomendasikan: `hyperliquid_validate_candidate_wallet`
- `HYPERLIQUID_WHALE_WATCHLIST` manual by design
- Validasi endpoint live sebelum commit

## Stage 4 — belum ditulis

- Observability eksternal (uptime + CF alerts) — lihat README "Yang MASIH belum ada"
- Generalisasi notifier: `src/telegram.ts` → `notify.ts` (multi-channel) — **belum ada file**
- Bukan refactor MCP jadi dashboard web

## Deferred

- Leverage Bracket riil — butuh API key Binance + desain

Jangan mulai Stage 3/4 tanpa spec/task eksplisit dari user.
